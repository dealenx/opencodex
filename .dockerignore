**

!package.json
!bun.lock
!tsconfig.json

!src/
!src/**
# Prepared on the host with the canonical Git-tracked-source generator.
!src/generated/compatibility-version.json

!scripts/
scripts/**
!scripts/model-metadata.source.json
!scripts/generate-build-info.ts
!scripts/prepare-package.ts
!scripts/generate-compatibility-version.ts

!docker/
!docker/**

!gui/
!gui/**
gui/node_modules/
gui/dist/
gui/.vite/

!gui/package.json
!gui/bun.lock

# Nixpacks (Dokploy/Railway) generates its build plan in .nixpacks/ at the repo
# root on the build host, then builds a Docker image from that context. The
# whitelist above would exclude it and fail the COPY with "not found".
!.nixpacks/
!.nixpacks/**

!bin/
!bin/**
!gui/dist/
!gui/dist/**
!gui/vite.config.ts
!gui/tsconfig.json
!gui/tsconfig.app.json
!gui/tsconfig.node.json
!gui/index.html
**/*.log
